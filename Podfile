# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'

target 'OpenChatApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenChatApp
  # Add any third-party dependencies here if needed

  # Example pods (uncomment if needed):
  # pod 'Alamofire'
  # pod 'SwiftyJSON'
  # pod 'SDWebImage'

  target 'OpenChatAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenChatAppUITests' do
    # Pods for testing
  end

end